"""
Plugin package – drop additional modules here to extend VulnScope.

Each plugin module may define a subclass of `VulnScopePlugin` and
register it via `register_plugin()` from `vulnscope.plugins.base`.
"""

